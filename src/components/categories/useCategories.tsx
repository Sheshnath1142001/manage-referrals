
import { useCategories as useRefactoredCategories } from '@/hooks/categories';

// Re-export the refactored hook with the same name to maintain compatibility
export const useCategories = useRefactoredCategories;
